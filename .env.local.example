# Copy this file to .env.local and fill in your own Cesium ion access token.
# Get a free token at https://ion.cesium.com/tokens (sign up, then "Create Token").
# .env.local is gitignored (matched by the *.local rule) — never commit a real token.
VITE_CESIUM_ION_TOKEN=your-token-here
